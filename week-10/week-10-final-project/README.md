#BOOK SEARCH CLI TOOL

##Features 
-Fetches book data using Open Library API
-Displays book title, author, and publication year
-Try/break error handlings for network conectivity and API errors
-Limits search resukts

##Installatio & Setup
-Python 3 is installed
-Installinf required dependencies:

"pip install -r requirements.txt" #
